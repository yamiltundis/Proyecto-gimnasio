
generator client {
  provider   = "prisma-client"
}

datasource db {
  provider = "mysql"
}

model Cliente {
  id     Int    @id @default(autoincrement())
  nombre String
  apellido String
  dni Int
  fechaNacimiento DateTime
  email  String @unique
  foto String
  rol String
}